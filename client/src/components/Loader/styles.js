const styles = theme => ({
  container: {
    height: '100%',
    width: '100%'
  },
  loading: {
    fontSize: '1rem',
    fontWeight: 500,
    paddingLeft: 15
  },
  flex: {
    margin: theme.spacing.unit * 2,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
    // color: '#00695c'
  }
});

export default styles;
