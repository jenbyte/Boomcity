const styles = theme => ({
  container: {
    height: '100vh',
    width: '100vw'
  },
  loading: {
    fontSize: '1rem',
    fontWeight: 600,
    marginRight: '1rem'
  },
  flex: {
    margin: theme.spacing.unit * 2,
    display: 'flex',
    flexDirection: 'center',
    alignItems: 'center'
    // color: '#00695c'
  }
});

export default styles;
